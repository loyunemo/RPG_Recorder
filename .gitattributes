# 统一换行符：仓库内一律使用 LF。
# 否则在 Windows 上提交、在 macOS/Linux 上拉取时会出现「整个文件都变了」的假 diff。
* text=auto eol=lf

# 二进制文件不做任何换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff2 binary
*.exe binary
*.dll binary

# 锁文件按生成物处理，不计入语言统计
package-lock.json linguist-generated=true
