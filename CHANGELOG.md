# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.5.0] — 未发布

### 新增
- **匕首心车卡数据补全**：从 [DHSheet · 匕首之心车卡器](https://dhsheet.site/)
  （[源码](https://github.com/RidRisR/DaggerHeart-CharacterSheet)）导入官方 SRD 的中文卡表
  - 血统 18 个种族 / 36 条特性
  - 社群 9 个，各带 1 条特性
  - 职业 9 个，含起始物品、希望特性、职业特性
  - 子职业 18 个（每职业 2 个），特性按基石/专精/大师分档，各带施法属性
  - 领域 9 个，**领域卡 189 张**（每领域 21 张）
- 角色卡新增「血统与社群特性」面板，把两条特性的完整效果显示出来
- 角色卡新增「领域卡」面板：按职业的两个领域筛选、可增删，并显示卡面文本
- 车卡向导的阵营/血统/社群改为**下拉选择**（原来是自由输入），职业选择会带出子职业
- 领域卡校验：**只能从本职职业的两个领域里选**，越界会被拦下
- `scripts/import-dhsheet.cjs`：可复跑的导入脚本，记录上游版本与署名到 `data/source.json`
- `scripts/build-data.cjs`：取代原 `build-huazhu-data.cjs`，两个规则集共用

### 修正
- **4 处职业领域译名与官方不符**：智慧→贤者、秘法→奥术、炽天使→神使、法典→典籍
- 演示战役的匕首心角色改用官方血统/社群/领域卡（原来用的是自造名称，会触发校验提示）

### 修复
- 角色卡插入规则校验面板时用了 `insertBefore(panel, body)`，但此时 `body` 还不是
  `container` 的子节点，抛 `NotFoundError` 导致整张卡面渲染中断
- 职业下拉读取 `c.domains.join()`，而华渚的职业用的是单数 `domain`，
  华渚卡面因此渲染到一半就崩（华渚复用了匕首心的卡面渲染）

### 说明
- 数据来源标注见 `src/core/rulesets/daggerheart/data/source.json`；
  上游仓库以 GPL-3.0 发布，卡牌内容派生自 DPCGL 授权的 Daggerheart SRD 1.0，
  中文翻译由 RidRisR、PolearmMaster、末楔、里予、一得完成，本项目保留完整署名
- 上游的 `automation` 字段（卡牌自动化引擎）**未导入** —— 那是另一套执行模型，
  与本项目的判定机制不兼容；本次只取其展示数据

---

## [0.4.0] — 2026-09-21

### 新增
- **按规则车卡向导**：新建角色不再是「填个名字然后全靠手填」，而是走四步流程
  ① 基础信息 → ② 属性 → ③ 技能与领域 → ④ 确认，全程显示预算与规则校验
- 四套规则各自的车卡约束：
  - **COC 7 版**：属性按规则书公式掷骰（力量/体质/敏捷/外貌/意志 3d6×5，
    体型/智力/教育 (2d6+6)×5）；年龄修正按规则书的额度下降；
    技能点分「本职」（教育×4）与「兴趣」（智力×2）两笔，本职技能最多 8 项
  - **DND 5 版**：属性用标准数组 / 点数购买（27 点）/ 4d6 弃最低 三选一；
    内置 12 职业（生命骰、豁免熟练、技能表与可选数量）与 13 种族（含半精灵的自选 +1×2）；
    校验 1 级生命值 = 生命骰满值 + 体质调整值
  - **匕首心**：起始六项属性必须恰好用掉 +2/+1/+1/0/0/−1 各一次；
    校验职业带来的闪避与生命点、起始希望 2 点、压力上限 6、护甲槽等于护甲分数；
    经历与领域卡数量按等级校验（1 级各 2）
  - **华渚**：在匕首心基础上，法门决定第一个领域、宗门流派给出第二个领域与施法属性；
    领域卡只能从这两个领域里选，越界会被拦下
- 角色卡页新增**规则校验面板**：偏离车卡规则时列出具体问题，但**只提示不阻止保存** ——
  房规、NPC、剧情需要都允许刻意偏离
- 新增 `src/core/creation.js`：车卡共用工具（点数购买换算、掷骰公式、校验结果组装）

### 说明
- 「自定义车卡接口」保留：向导只是新建时的引导，角色卡页仍可自由编辑任意字段
- COC 的职业表未内置（原文规则书未随附），因此改由玩家在车卡时标记「本职技能」，
  只有被标记的技能才能使用职业点数——这与规则书的点数分账结构一致

### 修复
- DND 职业技能校验的条件写反了（用 `&&` 连接两个否定），
  导致任何真实技能名都能通过，越界技能拦不住
- **年龄修正会把属性压到 0**（间歇性，约 25% 概率）：85 岁要减 80 点，
  掷骰较低时旧实现会把属性压到 0，而校验要求 ≥1 —— 车卡向导会卡在玩家无法修正的错误上。
  规则书要求减不动的额度分配给别的属性，现改为轮流各扣 1 点、见底的项跳过、额度顺延
- 车卡向导的校验提示被挤到折叠线以下、底部按钮会随内容滚走
- `package-lock.json` 的版本号落后于 `package.json`（还停在 0.2.0）

### 发布产物

| 文件 | 大小 | SHA256 |
| --- | --- | --- |
| `RPG-Recorder-Setup-0.4.0.exe` | 76.6 MB | `1DB8D5CECA78CF1C2E7D507C070A8ADB0DBFE30C6B05D49DB8A5E7C81A4CFD86` |
| `RPG-Recorder-0.4.0-portable.exe` | 76.4 MB | `DFB906F8BB7C753B1E65481E00BADACD59851499702AE968C30D1C2AFB1900E1` |

两个版本都是 64 位 Windows 程序，共用 `%APPDATA%\rpg-recorder\data` 作为数据目录。
安装包未做代码签名，首次运行会被 SmartScreen 拦一下。

---

## [0.3.0] — 2026-09-21

### 新增
- **安装与部署文档**（`INSTALL.md`）：从源码安装、依赖说明、打包发布、常见问题
- **打包发布**：`npm run dist` 生成 Windows 安装版与便携版，附应用图标（`build/icon.png`）
- **MIT 许可**（程序代码；内置规则数据的归属另见 `LICENSE` 末尾）
- 新增渲染层接口一致性测试：视图里每个 `app.*` 调用都必须能在 `app.js` 的导出对象里找到，
  并校验所有相对导入路径存在

### 发布产物

| 文件 | 大小 | SHA256 |
| --- | --- | --- |
| `RPG-Recorder-Setup-0.3.0.exe` | 76.6 MB | `9E0591F76A1FF70FF144D3368CECF7745D3211B3DB9C2334D7A36AFDB36DFAB2` |
| `RPG-Recorder-0.3.0-portable.exe` | 76.4 MB | `EC68833CD85B6252C1430CD1FAFBF045C47FF60A5517081E66AF06E2227BF84D` |

两个版本都是 64 位 Windows 程序，共用 `%APPDATA%\rpg-recorder\data` 作为数据目录。
安装包未做代码签名，首次运行会被 SmartScreen 拦一下。

### 修复
- `app.canEditCharacter` 定义了却没加进导出的 `app` 对象，玩家打开角色卡页会崩
- 玩家未认领角色卡时点投掷会收到看不懂的 403；现在提前拦下并说明原因
- 战斗追踪器给玩家渲染完整控制台，一点就 403；现在改为只读战况视图
- 笔记页对玩家暴露场次管理、战役改名、数据目录；现在只保留只读信息与记场景
- 角色卡页对玩家暴露新建 / 删除按钮；看别人的卡时整页只读并给出提示
- 加入界面的「已在场」会列出所有历史席位导致重名；现在只列当前在线的人
- 加入阶段顶栏还挂着空的战役 / 场次下拉

### 变更
- 侧栏区分「我的 / 只读」角色卡
- `.gitignore` 改用根锚定的 `/data/`，避免误伤规则的 `data` 目录

---

## [0.2.0] — 多人牌桌与华渚框架

### 新增
- **多人实时牌桌**：主持人在桌面端开桌，玩家用浏览器接入同一局域网地址即可同玩。
  服务端→客户端用 SSE 推送、客户端→服务端用 POST，未引入 WebSocket 依赖
- **权限隔离**：玩家只能改自己认领的角色卡、只能掷自己角色的骰，
  不能删战役 / 建场次 / 管战斗
- **暗骰**：`visibility=gm` 的事件既不下发给玩家，也不出现在玩家的日志里
- 角色卡认领机制，一张卡同时只能被一人占用；席位跨场次保留
- 牌桌未开启时服务只接受本机请求，局域网内无法访问数据
- **华渚（中式奇幻·匕首之心框架）**：新增第四套规则集
  - 13 法门 / 55 宗门流派 / 345 条分档特性
  - 27 种族 / 15 社群 / 15 领域 315 张领域卡
  - 战役机制：位阶推导、道心经历、华渚声望、九玄技、领域卡管理
  - 装备按官方中文规则书交叉核对补齐 64 武器 / 9 副武器 / 17 护甲的真实数值

### 修复
- `store.appendEvents` 盖章事件时丢失 `visibility` 字段，导致暗骰对玩家可见
- `setTrackValue` 对匕首心 / 华渚角色扣血会崩溃（这两种系统用 `hpMarked`）

### 变更
- 顶部标签页增加「战斗」，现有四页：判定 / 战斗 / 角色卡 / 战役笔记

---

## [0.1.0] — 首个版本

### 新增
- **骰子引擎**：完整骰式语法（取高弃低、爆炸、重掷、上下限、成功计数、命运骰），
  每次掷骰带随机种子，日志可一键复现校验
- **三套规则**：克苏鲁的呼唤 7 版 / 龙与地下城 5 版 / 匕首心
  - COC：奖励骰与惩罚骰、三档难度、对抗检定、孤注一掷
  - DND：优势与劣势、熟练与专精、法术位、死亡豁免
  - 匕首心：双重骰、希望与恐惧、会心一击、压力与护甲槽
- **战斗追踪器**：先攻序列、回合推进、伤害与状态，PC 生命变化同步回角色卡
- **事件日志**：追加式 JSONL，记录掷骰、改卡 diff、笔记、场景、战斗全部事件
- **角色卡面板**：三套系统各自的卡面，派生数值自动计算
- 桌面版（Electron）与浏览器版（本地服务）共用同一套核心模块
- Markdown 复盘导出、战役存档导出

[0.3.0]: https://github.com/loyunemo/RPG_Recorder/compare/v0.2.0...HEAD
[0.2.0]: https://github.com/loyunemo/RPG_Recorder/releases/tag/v0.2.0
[0.1.0]: https://github.com/loyunemo/RPG_Recorder/releases/tag/v0.1.0
